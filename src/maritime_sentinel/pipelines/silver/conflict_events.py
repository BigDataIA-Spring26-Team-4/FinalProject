"""Silver transformation for ACLED conflict events."""


def transform_conflict_events() -> int:
    """Bronze → Silver: filter coastal events, normalize types, geo-tag to chokepoints."""
    raise NotImplementedError
