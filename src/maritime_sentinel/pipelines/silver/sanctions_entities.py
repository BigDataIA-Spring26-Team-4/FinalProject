"""Silver transformation for OFAC sanctions data."""


def transform_sanctions_entities() -> int:
    """Bronze → Silver: extract vessel-specific fields (IMO, MMSI), normalize names."""
    raise NotImplementedError
